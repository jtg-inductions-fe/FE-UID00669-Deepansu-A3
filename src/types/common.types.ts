export type City = {
    name: string;
};

export type Movie = {
    id: string;
    name: string;
    image_url: string;
    description: string;
    duration: number;
    release_date: string;
    genres: string[];
    language: string;
};
