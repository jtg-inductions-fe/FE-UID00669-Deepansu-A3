/**
 * @constant LOGIN_ERRORS all available login form errors
 */
export const LOGIN_VALIDATION_ERRORS = {
    INVALID_EMAIL: 'Email is invalid',
    REQUIRED_EMAIL: 'Email is required',
    REQUIRED_PASSWORD: 'Password is required',
} as const;
