import { CommonSliceType } from './common.slice.types';

/**
 * Initial state of the slice based on common slice type
 */
export const COMMON_SLICE_INITIAL_STATE: CommonSliceType = {
    /**
     * City details
     */
    city: null,
};
